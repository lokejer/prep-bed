<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- link bootstrap / bs icons / bs js -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

  <!-- link stylesheets -->
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="/newWorkout.css">
  <script defer src="/newWorkout.js"></script>

  <title>Workouts</title>
</head>
<body>

  <main>

    <section class="container-lg py-5 d-flex flex-column align-items-center">
      
      <a class="mb-4" href="/workouts">See All Workouts</a>
      <h2>Welcome Back, <%= locals.name %>! Log each exercise below.</h2>

      <div class="col-md-6 col-10 d-flex flex-column my-5 px-3 py-5 border border-dark rounded-3 shadow-sm">
      
        <form
          action="/workouts/add-exercise" 
          method="POST"
        >
          
          <label for="exerciseName">Name & Weight: </label>
          <input class="form-control mb-1" type="text" id="exerciseName" name="exerciseName" placeholder="e.g Bicep Curls" required>
          <input class="form-control mb-3" type="text" id="exerciseWeight" name="exerciseWeight" placeholder="e.g 20 (kg) or BW (bodyweight)">

          <label for="set1">Set 1: </label>
          <input class="form-control" type="number" id="set1" name="set1" placeholder="e.g 12 (reps)" required>
          <br>

          <label for="set2">Set 2: </label>
          <input class="form-control mb-1" type="number" id="set2" name="set2" placeholder="e.g 10 (reps)">
          <br>

          <label for="set3">Set 3: </label>
          <input class="form-control mb-1" type="number" id="set3" name="set3" placeholder="e.g 8 (reps)">
          <br><br>

          <button class="btn btn-light border border-dark w-100 mb-2 py-2 rounded-3" type="submit">Add Exercise</button>
          
        </form>

        <form action="/workouts/create-workout" method="POST">
          <button class="btn btn-outline-success w-100 mb-2 py-2 rounded-3 fw-bold" type="submit">Create Workout</button>
        </form>

      </div>

    </section>

  </main>

  <!-- toast container (successfully added exercise notification) -->
  <% if (added === '1') { %>
  <div class="toast-container position-fixed bottom-0 end-0 p-3">
    <div id="exerciseToast" class="toast align-items-center text-bg-success border-0 bg-opacity-75" role="alert" aria-live="assertive" aria-atomic="true">
      <div class="d-flex">
        <div class="toast-body">
          Exercise added successfully!
        </div>
        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
      </div>
    </div>
  </div>
  <% } %>

</body>
</html>